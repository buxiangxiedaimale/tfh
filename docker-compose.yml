services:
  flowtodo:
    build:
      context: .
      args:
        NODE_IMAGE: ${NODE_IMAGE:-docker.m.daocloud.io/library/node:20-alpine}
    image: flowtodo:latest
    container_name: flowtodo
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env.local
